# Restaurant-API
# 🍴 Restaurant API (Flask + MySQL)

A modern **Restaurant Management API** built with **Flask**, **MySQL**, **JWT Authentication**, **Sessions**, and **JSON/XML support**.  
Includes a clean web UI for authentication and menu management plus API responses for integration.

---

## 🚀 Features

- 🔐 User Authentication (Register & Login)
- 🔑 JWT-based authorization (Header or Session)
- 🧾 Menu Management (CRUD)
- 🌐 Supports **JSON** and **XML** responses
- 🖥️ Modern HTML UI (Login, Register, Menu Cards)
- 🍕 Food emoji auto-detection
- 🛡️ Protected routes with decorators
- 🗄️ MySQL database integration

---

## 🛠️ Tech Stack

- **Backend:** Flask (Python)
- **Database:** MySQL
- **Auth:** JWT + Flask Sessions
- **Security:** Bcrypt password hashing
- **Formats:** JSON & XML
- **Frontend:** HTML + CSS (inline templates)

---

## 📂 Project Structure

